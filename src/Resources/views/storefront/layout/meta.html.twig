{% sw_extends '@Storefront/storefront/layout/meta.html.twig' %}

{% block layout_head_stylesheet %}
    {{ parent() }}

    {% if not ecbConfEqualsConst('integrationMode', 'VALUE_INTEGRATION_MODE_NONE') %}
        <link rel="preload" as="image" href="{{ absolute_url('/bundles/eyecookblurhash/storefront/assets/images/ecb-spinner.svg') }}">
    {% endif %}

    {% if ecbConfEqualsConst('integrationMode', 'VALUE_INTEGRATION_MODE_CUSTOM') %}
        <script src="{{ asset('ecb-decode.js', '@EyecookBlurhash') }}"></script>
    {% elseif ecbConfEqualsConst('integrationMode', 'VALUE_INTEGRATION_MODE_EMULATED') %}
        <script src="{{ asset('ecb-emulated.js', '@EyecookBlurhash') }}"></script>
    {% endif %}

{% endblock %}
